import { Connection, PublicKey, Transaction } from '@solana/web3.js';
import type { Program } from '@project-serum/anchor';
import { AuctionHouseProgramIDL } from '../idl';
/**
 * Create Cancel Listing Transaction
 * @param mint
 * @param currentListingPrice
 * @param sellerPublicKey
 * @param auctionHouse
 * @param connection
 * @param auctionHouseAuthority
 * @param program
 * @param __DANGEROUSLY_INSET_SELLER__
 */
export declare function createCancelListingTransaction(mint: PublicKey, currentListingPrice: number, sellerPublicKey: PublicKey, auctionHouse: PublicKey, program: Program<AuctionHouseProgramIDL>, auctionHouseAuthority: PublicKey, connection: Connection, __DANGEROUSLY_INSET_SELLER__?: string): Promise<readonly [Transaction, PublicKey]>;
