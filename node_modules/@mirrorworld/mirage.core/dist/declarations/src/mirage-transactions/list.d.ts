import { PublicKey, Transaction } from '@solana/web3.js';
import type { Program } from '@project-serum/anchor';
import { AuctionHouseProgramIDL } from '../idl';
/**
 * Create List Transaction
 * @param mint
 * @param listingPrice
 * @param sellerPublicKey
 * @param auctionHouse
 * @param program
 */
export declare function createListingTransaction(mint: PublicKey, listingPrice: number, sellerPublicKey: PublicKey, auctionHouse: PublicKey, program: Program<AuctionHouseProgramIDL>): Promise<readonly [Transaction, PublicKey]>;
