import { Connection, PublicKey, Transaction } from '@solana/web3.js';
import { Program } from '@project-serum/anchor';
import { AuctionHouseProgramIDL } from '../idl';
export declare function createTransferInstruction(mint: PublicKey, recipient: PublicKey, holderPublicKey: PublicKey, auctionHouse: PublicKey, auctionHouseAuthority: PublicKey, program: Program<AuctionHouseProgramIDL>, connection: Connection): Promise<Transaction>;
