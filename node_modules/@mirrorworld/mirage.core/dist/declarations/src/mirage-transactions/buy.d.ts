import { Connection, PublicKey, Transaction } from '@solana/web3.js';
import type { Program } from '@project-serum/anchor';
import { AuctionHouseProgramIDL } from '../idl';
/**
 * Create Buy Transaction object
 * @param mint
 * @param buyerPrice
 * @param buyer
 * @param auctionHouse
 * @param program
 * @param connection
 */
export declare function createBuyTransaction(mint: PublicKey, buyerPrice: number, buyer: PublicKey, auctionHouse: PublicKey, program: Program<AuctionHouseProgramIDL>, connection: Connection): Promise<readonly [Transaction, PublicKey]>;
