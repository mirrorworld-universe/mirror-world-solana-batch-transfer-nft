import { Connection, PublicKey, Transaction } from '@solana/web3.js';
import type { Program } from '@project-serum/anchor';
import { AuctionHouseProgramIDL } from '../idl';
/**
 * Create Update Listing Transaction
 * @param mint
 * @param currentListingPrice
 * @param newListingPrice
 * @param sellerPublicKey
 * @param auctionHouse
 * @param connection
 * @param auctionHouseAuthority
 * @param program
 */
export declare function createUpdateListingTransaction(mint: PublicKey, currentListingPrice: number, newListingPrice: number, sellerPublicKey: PublicKey, auctionHouse: PublicKey, program: Program<AuctionHouseProgramIDL>, auctionHouseAuthority: PublicKey, connection: Connection): Promise<readonly [Transaction, PublicKey]>;
