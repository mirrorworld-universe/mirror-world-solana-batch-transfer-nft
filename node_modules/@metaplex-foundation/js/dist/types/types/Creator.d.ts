import { PublicKey } from '@solana/web3.js';
import { Signer } from './Signer';
export declare type Creator = Readonly<{
    address: PublicKey;
    verified: boolean;
    share: number;
}>;
export declare type CreatorInput = Readonly<{
    address: PublicKey;
    share: number;
    authority?: Signer;
}>;
